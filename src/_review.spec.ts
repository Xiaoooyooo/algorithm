import assert from "assert";
import fn from "./_review";

// describe("_review", () => {
//   it.only("go", () => {
//     assert.deepEqual(fn([1, 3, 2, 8, 4, 9], 2), 8);
//     assert.deepEqual(fn([1, 3, 7, 5, 10, 3], 3), 6);
//   });
// });
