import assert from "assert";
import fn from "./_review";

// describe("_review", () => {
//   it.only("go", () => {
//     assert.deepEqual(fn([5, 7, 7, 8, 8, 10], 8), [3, 4]);
//     assert.deepEqual(fn([5, 7, 7, 8, 8, 10], 6), [-1, -1]);
//     assert.deepEqual(fn([], 0), [-1, -1]);
//   });
// });
